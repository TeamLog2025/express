doctype html
html
    head
        title Add
    body
        h1 도서 추가
        form(action="/add" method="POST")
            input(type="text" name="title" placeholder="제목") 
            br
            input(type="text" name="author" placeholder="저자") 
            br
            input(type="text" name="year" placeholder="출판연도") 
            br
            button(type="submit") 추가